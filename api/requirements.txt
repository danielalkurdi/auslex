# Core FastAPI dependencies - optimized for Vercel serverless
fastapi==0.104.1
pydantic[email]==2.5.0

# Authentication and security
PyJWT==2.8.0
passlib[bcrypt]==1.7.4

# OpenAI integration
openai==1.35.7

# HTTP client - lightweight alternative to requests
httpx==0.27.0

# Optional: Add these only if needed for specific features
# uvicorn==0.24.0  # Not needed on Vercel
# gunicorn  # Not needed on Vercel